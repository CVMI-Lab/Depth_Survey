dataset: ScannetImgDataset
dataset_params:
  dir: /mnt/pfs/data/RGBD/marigold_eval/scannet/scannet_val_sampled_800_1.tar
  disp_name: scannet_val_800_1
  filenames: dataset/scannetImg/scannet_val_sampled_list_800_1.txt

model_name: "Marigold"
model_params:
  model_dir: "/mnt/pfs/users/sunyangtian/Depth/Marigold"
  ckpt_path: "/mnt/pfs/share/pretrained_model/marigold-depth-v1-1"
  denoise_steps: 1
  ensemble_size: 1
  half_precision: False
  processing_res: 0
  output_processing_res: False
  resample_method: bilinear
  # seed: None
  color_map: Spectral

eval_depth:
  metric_names: 
    - 'Abs Rel'
    - 'delta < 1.25'
    - 'delta < 1.25^2'
    - 'delta < 1.25^3'
  depth_alignment: "lstsq"

vis_depth: True